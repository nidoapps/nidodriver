# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

exec < /dev/tty && npx cz --hook || true
#!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# if [ $2 == "template" ]; then
#   exec < /dev/tty && npx cz --hook || true
# fi