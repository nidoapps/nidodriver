export enum ColorSchema {
  light = 'light',
  dark = 'dark',
}

export enum StudentStopStatus {
  completed = 'completed',
  absent = 'absent',
}

export enum StopStatus {
  pending = 'pending',
  active = 'active',
  completed = 'completed',
}
